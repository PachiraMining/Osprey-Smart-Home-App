name: smart_curtain_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.2+6

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  get_it: ^7.3.0
  dio: ^5.3.2
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  permission_handler: ^11.3.1
  flutter_blue_plus: ^1.14.0
  # BLE pairing crypto: HMAC-SHA256 + HKDF (Osprey pairing protocol)
  cryptography: ^2.7.0
  # AES-128-CCM cho PAIRING_DATA — package:cryptography KHÔNG có CCM
  # (handoff doc gợi ý AesCcm nhưng class đó không tồn tại trong pkg đó)
  pointycastle: ^3.9.1
  # clock cho fake_async testing CloudHealthCubit (debounce 10s/30s)
  clock: ^1.1.1
  # Tự detect SSID WiFi đang kết nối (màn nhập WiFi khi pairing)
  network_info_plus: ^6.1.4
  # Product catalog local cache (BLE pairing)
  shared_preferences: ^2.2.0
  get: ^4.6.6
  equatable: ^2.0.5
  # HTTP
  http: ^1.1.0
  
  # Secure Storage
  flutter_secure_storage: ^9.0.0

  # QR Code Scanner
  mobile_scanner: ^6.0.2
  webview_flutter: ^4.13.1
  lottie: ^3.1.0
  flutter_svg: ^2.0.10
  flutter_web_auth_2: ^5.0.1
  url_launcher: ^6.2.0
  dart_jsonwebtoken: ^2.12.0
  quickalert: ^1.1.0

  # Crash reporting & performance monitoring
  sentry_flutter: ^8.9.0

  # MQTT for real-time IoT device telemetry/state
  mqtt_client: ^10.5.1

  # Custom typography (Plus Jakarta Sans)
  google_fonts: ^6.2.1

  # AI / Voice (Curtain AI features)
  speech_to_text: ^7.0.0
  sqflite: ^2.3.3
  path_provider: ^2.1.4
  geolocator: ^13.0.2
  home_widget: ^0.9.3
  flutter_timezone: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
  flutter_native_splash: ^2.4.4

# Native launch screen — kept in lock-step with the Flutter SmartSplashScreen so
# the OS splash and the animated Flutter splash look like ONE screen (same
# background #F2F7FC = AppColors.background, same osprey_life logo). Regenerate
# with: dart run flutter_native_splash:create
flutter_native_splash:
  color: "#F2F7FC"
  image: assets/splash_logo_padded.png
  color_dark: "#F2F7FC"
  image_dark: assets/splash_logo_padded.png
  android_12:
    color: "#F2F7FC"
    image: assets/splash_logo_padded.png
    icon_background_color: "#F2F7FC"
    color_dark: "#F2F7FC"
    image_dark: assets/splash_logo_padded.png
    icon_background_color_dark: "#F2F7FC"
  android: true
  ios: true
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/eagle_logo.png
    - assets/osprey_avatar.png
    - assets/osprey_life_logo.png
    - assets/splash_bg.png
    - assets/icons/google_logo.png
    - assets/icons/apple_logo.png
    - assets/icons/alexa_logo.png
    - assets/icons/google_assistant_logo.png
    - assets/icons/curtain_open.png
    - assets/icons/curtain_close.png
    - assets/icons/curtain_track.png
    - assets/icons/bluetooth_disconnect.svg
    - assets/lottie/light_bulb.json
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
